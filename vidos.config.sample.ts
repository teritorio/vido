import type { VidosConfig } from '~/utils/types-config'

const config: VidosConfig = {
  localhost: {
    HOSTS: ['localhost'],
    API_PROJECT: 'dev',
    API_THEME: 'tourism',
  },
}

export default config
