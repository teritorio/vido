version: '3'

services:
  vido:
    build:
      context: .
      args:
        NODE_VERSION: 16-alpine3.16
    # restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./.env:/usr/src/app/.env
      - ./vidos-config.json:/usr/src/app/vidos-config.json
